# Byte-compiled
__pycache__/
.cache/*
staticfiles/*
*.pyc

env/*
# sensitive data
# deprecated, use environment for docker or kubernetes secrets
dboeannotation/settings/server.py
dboeannotation/settings/dev.py

#sqlitedb
db.sqlite3

# IDE
.idea

# the only file we want to have in git for migrations is __init__.py
annotations/migrations/*


#Ipython Notebook
.ipynb_checkpoints

# the only file we want to have in git for settings is basex.py
dboeannotation/settings/*
